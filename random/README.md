# Quick and easy programs I make for small tasks

## Linked Card Redstone

A program to link redstone signals between two computers using linked cards. One computer runs in "send" mode, sending its redstone input states to the other computer, which runs in "watch" mode and sets its redstone outputs accordingly.

$ `linkedcardredstone [send|watch]`